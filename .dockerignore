.git
.gitignore
.jj
.pixi
.venv
.gemini
.ruff_cache
__pycache__
*.pyc
*.pyo
*.pyd
*.egg-info
build
dist
.env
logs
docs
notebooks
satellite-recordings
gp.php

# Frontend has its own build context — excluded from backend/simulator builds
frontend

# Data is bind-mounted at runtime, not baked into images
data

# Models are bind-mounted at runtime (read-only)
models

# DB init scripts are bind-mounted, not needed in Python images
db/init
